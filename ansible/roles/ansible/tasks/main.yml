---

- name: Install pip
  apt: name=python3-pip state=present update_cache=true force=yes
  become: yes

- name: install ansible
  pip:
    name: ansible
    version: 2.10.7
  notify: disable hostkey for ansible
  become: yes
